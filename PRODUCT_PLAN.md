# SO101 轻量级工位执行终端产品规划

## 产品定位

SO101 轻量级工位执行终端，是面向低配现场电脑的机械臂示教、发布、执行与追溯系统。

它不依赖 ROS，不绑定摄像头，不引入复杂中间件，而是在现有录制/回放能力之上，建立一层清晰的企业作业语义：把“动作文件”转化为“产品动作”，把“按钮操作”转化为“任务执行”，把“现场经验”沉淀为可发布、可迁移、可追溯的轻量资产。

## 核心理念

企业现场不需要看到一堆 `record_xxx` 文件，也不需要理解底层配置。

现场真正需要的是：

```text
今天做什么任务
用哪个产品动作
执行了多少次
有没有异常
动作能不能备份迁移
```

因此产品抽取 4 个核心概念：

```text
任务 Job
产品动作 Product Action
发布状态 Release State
执行记录 Run Log
```

## 一、产品动作层

当前系统已经有录制和回放，但录制动作偏技术层。下一步要增加“产品动作”概念。

录制动作：

```text
record_20260622_161439
record_teleop_20260622_161003
```

产品动作：

```text
左侧抓取
右侧放料
上料动作 A
检测位移动
```

产品动作绑定一个录制动作，并带有简洁业务信息：

```text
动作名称
来源录制
备注
状态：草稿 / 已发布 / 停用
发布人
更新时间
```

这样员工面对的是“左侧抓取”，不是技术文件名。

## 二、动作发布流程

动作不再是“录完就混在列表里直接用”，而是形成轻量发布流程：

```text
录制动作 -> 测试播放 -> 保存为产品动作 -> 发布 -> 员工执行
```

动作状态只保留三个，避免复杂：

```text
草稿：调试中的动作
已发布：员工可执行的动作
停用：不再使用，但保留记录
```

这个设计保留企业需要的规范性，同时不增加过多心智负担。

## 三、任务模式

任务模式是 MES 思想的最小抽取。

不是做完整 MES，而是把 MES 里最关键的一条链融合进系统：

```text
任务 -> 动作 -> 操作员 -> 执行 -> 结果
```

员工界面只显示当前任务：

```text
当前任务：产品 A
产品动作：左侧抓取
目标次数：20
已完成：7
按钮：执行一次 / 暂停 / 异常
```

员工不需要选复杂动作库，也不需要理解底层文件。

## 四、运行记录

运行日志只记录企业管理真正关心的关键事件：

```text
谁
什么时候
执行了哪个任务
执行了哪个产品动作
成功 / 停止 / 失败
异常原因
```

调试日志和业务日志分离：

```text
业务日志：简洁，给企业看
调试日志：详细，给维护排障看
```

这样既保留追溯能力，又不让界面变重。

## 五、产品动作包

数据备份不直接暴露 `config/`、`recordings/` 这些技术文件，而是包装成“产品动作包”。

一个动作包包含：

```text
产品动作定义
绑定的录制轨迹
备注
发布状态
发布人
发布时间
```

企业现场理解的是：

```text
这是一套可用动作，可以备份、迁移、恢复。
```

不是一堆 JSON 文件。

## 建议界面结构

后续产品界面建议收敛成三个入口：

```text
员工执行
动作发布
系统维护
```

员工执行：

- 当前任务
- 当前产品动作
- 执行一次
- 完成计数
- 异常上报

动作发布：

- 从录制动作创建产品动作
- 命名、备注
- 测试播放
- 发布 / 停用
- 导出动作包

系统维护：

- 登录记录
- 简洁运行日志
- 动作包导入 / 导出
- 设备状态

## 产品一句话

把机械臂录制动作，从“调试文件”升级为“可发布、可执行、可追溯、可迁移的企业现场动作资产”。
