# 中期报告正文

## 一、开题以来所作的具体工作及取得的进展

1. 资料研究与方案设计：围绕SO101六自由度机械臂从臂控制系统的设计目标，查阅了教学型机械臂、Feetech STS总线舵机通信协议、Python本地HTTP服务、Linux串口通信等相关资料，明确了系统不依赖ROS2、Node.js、PyTorch等大型框架的总体方向。结合实训室Linux Mint运行环境和机械臂演示需求，确定采用“浏览器前端、本地HTTP服务、驱动抽象层、串行总线、执行机构”的分层架构，为后续软件开发和实机调试奠定了基础。

2. 硬件平台确认与通信方案搭建：完成SO101机械臂从臂硬件结构、六个STS3215舵机ID、供电方式、USB串口适配器和舵机总线连接方式的确认。根据Linux Mint系统下的设备识别特点，优先采用`/dev/serial/by-id`稳定串口路径，避免设备重插后`/dev/ttyACM0`、`/dev/ttyUSB0`编号变化导致连接失败。通过读取型号编号寄存器的方式，对ID 1至6的舵机进行在线扫描和型号校验，提高了实机连接前的安全性。

3. 串口通信与协议程序开发：完成基于Python标准库的Linux原生串口通信实现，使用`termios`和`select`完成串口打开、参数配置、读写超时控制等功能，不依赖`pyserial`等第三方库。在此基础上，自主实现Feetech STS通信协议的核心指令，包括Ping、Read、Write和Sync Write，能够完成舵机在线检测、寄存器读取、力矩使能控制、目标位置同步下发和当前位置读取等操作。

4. 软件系统框架与Web界面开发：完成本地HTTP服务器和Web前端界面的初步搭建。后端基于Python标准库`http.server`实现接口路由、JSON数据处理、静态页面托管和多线程请求处理；前端使用HTML、CSS和JavaScript实现连接控制、状态显示、动作执行、录制回放、产品动作管理和主从遥操作入口。系统已能够通过浏览器访问本地控制台，完成主要操作流程的界面化展示。

5. 坐标映射与安全控制实现：完成ROS关节空间、LeRobot动作空间和STS原始编码值之间的三级映射关系设计。结合标定文件和关节方向系数，对肩部、肘部、腕部和夹爪等关节的位置转换进行了调试。系统加入了dry-run安全模式、关节限幅、最大相对运动角度限制、连接阶段电机型号校验、停止与释放力矩等机制，降低了实机调试过程中的误操作风险。

6. 动作录制、产品动作与运行记录功能开发：在基础动作模板执行功能之外，增加了录制动作保存、录制动作回放、产品动作发布、产品动作执行、登录记录和业务运行日志等功能。该部分将底层录制文件进一步封装为便于现场使用的产品动作，使操作者不必直接面对复杂的技术文件名，有利于后续毕业设计展示和现场演示。

7. 主从遥操作功能调试：完成主臂串口配置、主臂ID扫描、主从当前位置对齐、主从遥操作启动、暂停、继续和停止等基础功能开发。通过`teleop_calibration.json`保存主臂与从臂在同一姿态下的原始位置数据，用于修正两套机械臂之间的安装差异。目前主从跟随功能已经具备基础演示能力，但跟随平滑性、夹爪映射精度和异常恢复机制仍需继续优化。

8. 阶段性调试结果：目前系统已经能够在Linux Mint环境下启动本地Web控制台，完成串口连接、舵机扫描、状态读取、动作回放、产品动作执行和主从遥操作等主要功能验证。调试过程中发现并处理了串口权限不足、设备路径变化、串口响应超时、电机型号校验失败、关节方向不一致、夹爪开合范围不合适等问题，形成了初步的故障排查思路和参数调整方法。

## 二、下一步主要工作

1. 完善实机调试与稳定性验证：继续对SO101机械臂从臂进行长时间运行测试，重点检查串口通信稳定性、动作执行连续性、停止与释放力矩逻辑、异常情况下的恢复能力。针对不同动作速度、不同运动幅度和不同串口超时参数进行对比测试，进一步确定适合现场演示的稳定参数。

2. 优化主从遥操作效果：继续调整主从遥操作中的位置对齐参数、单步限速、最大跟随偏差和夹爪映射关系，使从臂动作更加平滑、稳定、可控。重点解决跟随过程中可能出现的抖动、延迟、夹爪开合不一致和异常停止后的恢复问题，提高主从控制的演示效果。

3. 完善产品动作流程：继续优化录制动作到产品动作的转换流程，规范产品动作名称、来源录制、发布状态、备注和运行记录等信息。进一步检查产品动作执行次数、执行结果记录、异常信息保存等功能，使系统不仅能完成机械臂动作演示，还能体现轻量级现场作业管理的设计思路。

4. 完善Web界面与操作提示：根据现场演示需求，进一步调整Web界面的信息层级和按钮逻辑，使连接、初始化、录制、回放、产品动作执行、主从遥操作等流程更加清晰。重点优化真实硬件模式下的风险提示、状态显示和错误信息，使操作者能够快速判断当前系统状态和下一步操作。

5. 补充测试记录与运行日志分析：整理dry-run模式、实机模式、产品动作执行和主从遥操作过程中的测试记录，分析成功执行次数、异常类型、串口超时情况和参数调整结果，为毕业设计论文中的系统调试章节提供真实依据。

6. 完成论文后续撰写与资料整理：在现有系统实现基础上，继续完善毕业设计论文的软件设计、通信协议实现、系统调试、故障分析和总结展望等章节。整理项目源码、配置文件、操作说明、运行截图和演示流程，形成完整的毕业设计成果材料。

7. 准备最终展示与答辩：根据毕业设计展示要求，准备一套稳定可重复的演示流程，包括系统启动、连接从臂、状态读取、产品动作执行、录制回放、主从遥操作和异常停止演示。提前检查机械臂供电、串口连接、浏览器访问、配置文件和动作数据，保证最终答辩展示过程稳定可靠。
