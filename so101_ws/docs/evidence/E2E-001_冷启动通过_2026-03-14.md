# E2E-001 冷启动通过记录（2026-03-14）

## 结论：通过

`tools/e2e/start_e2e_stack.sh` 已在零基线条件下完成单命令冷启动，成功拉起：

- ROS2 仿真链路
- rosbridge (`9090`)
- Spring Boot backend (`8080`)
- Vue Vite frontend (`5173`)

随后使用 `tools/e2e/stop_e2e_stack.sh` 完成清理。

## 前置条件

冷启动前已确认端口均未被占用：

```text
9090: closed
8080: closed
5173: closed
```

## 实际执行命令

```bash
cd /home/muqiao/dev/ros2/workspaces/so101_ws
bash tools/e2e/start_e2e_stack.sh
```

完成后清理：

```bash
bash tools/e2e/stop_e2e_stack.sh
```

## 关键输出

```text
started ROS2 stack with PID 179413
rosbridge ready on port 9090
started backend with PID 179533
backend ready: http://127.0.0.1:8080/api/ros/health
started frontend with PID 179670
frontend ready: http://127.0.0.1:5173
e2e stack ready
frontend: http://127.0.0.1:5173
backend:  http://127.0.0.1:8080
rosbridge: ws://127.0.0.1:9090
```

停止输出：

```text
stopped frontend
stopped backend
stopped ros2 stack
```

## 关联日志

- `.vscode/.runtime/so101_e2e_ros.log`
- `.vscode/.runtime/so101_e2e_backend.log`
- `.vscode/.runtime/so101_e2e_frontend.log`

## 本次修复点

1. `tools/e2e/start_e2e_stack.sh`
   - 为 ROS2 launch 显式设置工作区内可写的 `ROS_LOG_DIR`
   - 解决默认写 `~/.ros/log` 导致的权限失败
2. `tools/e2e/start_e2e_stack.sh`
   - 将端口探测从 `/dev/tcp` 改为 `nc -z -w 1`
   - 解决关闭端口时探测阻塞、前端启动前卡住的问题
3. `src/so101_bringup/launch/sim_bringup.launch.py`
4. `src/so101_bringup/launch/real_bringup.launch.py`
   - 为 `detection_template_map_json` 增加显式字符串引号
   - 解决 `task_executor.py` 参数解析失败
