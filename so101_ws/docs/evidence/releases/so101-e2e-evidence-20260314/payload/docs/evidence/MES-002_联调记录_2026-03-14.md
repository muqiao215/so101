# MES-002 联调记录（2026-03-14）

## 目标

验证 `ROS2 /mes_task_status -> rosbridge -> mes_backend -> /topic/task-status -> mes_frontend` 链路是否真实可用，并记录可复现证据。

## 结论

- 当前按修正后的验收路径，`MES-002` 可判定为 `通过`。
- `ROS2 /mes_task_status -> rosbridge -> mes_backend -> /topic/task-status -> 前端实时显示` 已完成真实联调。
- 当前前端为 Vite 工程，正确复测方式是：
  - `cd mes_frontend && npm run dev`
  - 或托管 `dist/`
- `file://` 直接打开 `mes_frontend/index.html` 不是当前项目的有效验收路径，不作为 bug 处理。
- 历史上阻塞本任务的两个代码层问题均已修复并复验通过：
  - rosbridge 冷启动时显式携带 `type: "std_msgs/msg/String"` 后，可直接订阅 `/mes_task_status`
  - backend 已补 `/api/**` 的 REST CORS，前端经 `5173` 或 `8000` 访问时不再出现 `Failed to fetch`

## 实际执行命令

### 1. 启动 ROS2 侧

```bash
cd /home/muqiao/dev/ros2/workspaces/so101_ws
export ROS_LOG_DIR=/home/muqiao/dev/ros2/workspaces/so101_ws/log/ros_launch
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch so101_bringup sim_bringup.launch.py use_mock_yolo:=false enable_rosbridge:=true use_rviz:=false
```

### 2. 启动后端

```bash
cd /home/muqiao/dev/ros2/workspaces/so101_ws/mes_backend
mvn spring-boot:run
```

### 3. 后端健康检查

```bash
curl http://127.0.0.1:8080/api/ros/health
```

返回：

```json
{"connected":true}
```

### 4. 冷启动复验

不预热 topic，直接单发：

```bash
source /opt/ros/humble/setup.bash
source /home/muqiao/dev/ros2/workspaces/so101_ws/install/setup.bash
ros2 topic pub --once /mes_task_status std_msgs/msg/String \
  "{\"data\":\"{\\\"requestId\\\":\\\"req-evidence-001\\\",\\\"state\\\":\\\"执行中\\\",\\\"code\\\":\\\"STEP\\\",\\\"message\\\":\\\"2/8 -> above_pick\\\",\\\"ts\\\":1772188888123}\"}"
```

结果：可直接发布并被后端/前端接收，无需预热 topic。

### 5. 页面复验（Vite）

```bash
cd /home/muqiao/dev/ros2/workspaces/so101_ws/mes_frontend
npm run dev
```

然后注入新的单发消息：

```bash
source /opt/ros/humble/setup.bash
source /home/muqiao/dev/ros2/workspaces/so101_ws/install/setup.bash
ros2 topic pub --once /mes_task_status std_msgs/msg/String \
  "{\"data\":\"{\\\"requestId\\\":\\\"req-evidence-003\\\",\\\"state\\\":\\\"执行中\\\",\\\"code\\\":\\\"STEP\\\",\\\"message\\\":\\\"4/8 -> close_gripper\\\",\\\"ts\\\":1772188890001}\"}"
```

## 关键日志

### ROS2 / rosbridge 侧

修复后的冷启动订阅成功：

```text
[rosbridge_websocket]: [Client ...] Subscribed to /mes_task_status
```

rosbridge 正常启动：

```text
[rosbridge_websocket]: Rosbridge WebSocket server started on port 9090
```

### backend 侧

```text
Connected rosbridge: ws://127.0.0.1:9090
```

STOMP broker 正常：

```text
BrokerAvailabilityEvent[available=true, SimpleBrokerMessageHandler ...]
```

### WebSocket / STOMP 探针

历史第一次探针消息：

```text
TASK_STATUS_MESSAGE {"requestId":"req-evidence-001","state":"执行中","code":"STEP","message":"2/8 -> above_pick","ts":1772188888123}
```

修复后的冷启动复验消息：

```text
TASK_STATUS_MESSAGE {"requestId":"req-evidence-003","state":"执行中","code":"STEP","message":"4/8 -> close_gripper","ts":1772188890001}
```

## 前端验证

### A. 正确验收路径：Vite 开发服务器

文件：

- `/home/muqiao/dev/ros2/workspaces/so101_ws/mes_frontend/index.html`

说明：

- 该文件是 Vite 入口壳，应通过开发服务器或静态托管服务访问。
- `file://` 直接打开不属于当前验收路径。

Vite 实测结果：

- 页面可正常加载
- `ROSBRIDGE在线`
- `WEBSOCKET已连接`
- 实时事件流出现：

```text
状态回传
{"requestId":"req-evidence-003","state":"执行中","code":"STEP","message":"4/8 -> close_gripper","ts":1772188890001}
```

浏览器观测：

- `HAS_FAILED_FETCH false`
- `HAS_ROSBRIDGE_ONLINE true`
- `HAS_REQUEST true`

### B. 使用 `mes_frontend/dist` 进行 HTTP 托管

命令：

```bash
python3 -m http.server 8000 --directory /home/muqiao/dev/ros2/workspaces/so101_ws/mes_frontend/dist
```

结果：

- 页面可加载。
- 点击“连接 WebSocket”后，页面能实时显示 `状态回传`。
- 当前 backend 已放行 `/api/**` 的跨域访问，`dist` 托管时不再出现 REST CORS 阻塞。
- 注入 `req-evidence-002` 后，页面正文中可见：

```text
状态回传
{"requestId":"req-evidence-002","state":"执行中","code":"STEP","message":"3/8 -> descend_pick","ts":1772188888999}
```

## 当前判断

- `ROS2 -> rosbridge`：通过
- `rosbridge -> backend`：通过
- `backend -> /topic/task-status`：通过
- `mes_frontend` 通过 `npm run dev` 或托管 `dist`：通过

## 根因判断

### 已修复 1：rosbridge subscribe 显式携带 topic type

相关代码：

- `/home/muqiao/dev/ros2/workspaces/so101_ws/mes_backend/src/main/java/com/so101/mes/service/RosbridgeClientService.java`

当前 subscribe 发送的是：

```json
{"op":"subscribe","topic":"/mes_task_status","type":"std_msgs/msg/String"}
```

### 已修复 2：前端 REST CORS

相关文件：

- `/home/muqiao/dev/ros2/workspaces/so101_ws/mes_backend/src/main/java/com/so101/mes/config/RestCorsConfig.java`

当前已放行：

```text
http://127.0.0.1:* , http://localhost:*
```

### 非 bug 说明：`file://` 直开 `index.html`

- 该文件是 Vite 入口壳，设计上应由开发服务器或静态托管服务提供。
- 因此 `file://` 直接打开不作为当前项目的失败判据。

## 证据文件

- `/home/muqiao/dev/ros2/workspaces/so101_ws/docs/evidence/mes-002-file-open-failure.png`
- `/home/muqiao/dev/ros2/workspaces/so101_ws/docs/evidence/mes-002-http-page.png`
- `/home/muqiao/dev/ros2/workspaces/so101_ws/docs/evidence/mes-002-http-live.png`
- `/home/muqiao/dev/ros2/workspaces/so101_ws/docs/evidence/mes-002-vite-live-fixed.png`

## 给后续 Agent 的建议动作

当前 `MES-002` 已具备关闭条件。后续若继续完善，优先关注：

1. 清理 `sim_bringup.launch.py` 中与本任务无关的旁路故障：
   - `task_executor.py` 参数 YAML 解析错误
   - `gazebo_ros2_control/GazeboSystem` 插件缺失
2. 如果答辩文档仍写着“直接打开 `mes_frontend/index.html`”，应把描述改成 `npm run dev` 或托管 `dist`。

## 本次未改代码

- 本文件最后一次更新包含真实复验结论，但本 agent 在本轮仍只做验证和文档回写。
